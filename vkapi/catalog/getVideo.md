#### catalog > catalog.getVideo

Возвращает каталоги по интересам вкладки "Видео". Реки то есть. Возвращаются их ID и названия, а сами видео получаются через `catalog.getSection`.

##### Параметры

Неизвестны.
