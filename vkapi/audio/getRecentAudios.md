#### Audio > audio.getRecentAudios

Возвращает недавно прослушанные аудиозаписи пользователя.

##### Параметры

|Параметр|Описание|
|--|--|
|`user_id`|ID пользователя.|

##### Результат

Возвращает число недавно прослушанных аудиозаписей в поле `count`, и массив с ними в `items`.
