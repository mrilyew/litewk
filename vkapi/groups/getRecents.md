#### groups > groups.getRecents

Возвращает недавно посещённые тобой группы (если передал `track_code`.).

##### Параметры

Неизвестны.

##### Результат

Возвращает недавно посещённые тобой группы, в `count` — их число, в `items` — их объекты в массиве.
