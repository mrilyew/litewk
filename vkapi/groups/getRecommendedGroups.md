#### groups > groups.getRecommendedGroups

Возвращает рекомендуемые тебе группы.

##### Параметры

|Параметр|Описание|
|--|--|
|`count`|Необходимое число друзей|
|`offset`|Отступ|

##### Результат

Возвращает рекомендуемые тебе группы, в `count` — их число, в `items` — их объекты в массиве.
